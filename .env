PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=autoflow

# Set true only for local UI testing without MySQL.
SKIP_AUTH=true
# Use production only when serving over HTTPS.
NODE_ENV=development
